export interface BikeStructure {
  _id: string;
  brand: string;
  model: string;
  wheelsize: number;
  material: string;
  specs: string;
  imageUrl: string;
  alternativeText: string;
  mode: string;
}
